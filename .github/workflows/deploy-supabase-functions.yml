name: Deploy Supabase Functions

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/functions/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          supabase functions deploy generate-course-content --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
          supabase functions deploy analyze-slide --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
          supabase functions deploy unsplash-search --project-ref ${{ secrets.SUPABASE_PROJECT_ID }}
